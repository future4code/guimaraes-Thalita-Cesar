# CRIANDO A TABELA
CREATE TABLE instrutores (
	id INT PRIMARY KEY,
    nome VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE, 
    idade INT DEFAULT 18 # NULL
);

# DELETANDO A TABELA
DROP TABLE instrutores;

# INSERINDO VALORES NA TABELA
INSERT INTO instrutores (id, nome, email, idade) 
VALUES (6, "Tayanne", "th@labenu.com", 27);

	# SHORTCUT - ATALHO
INSERT INTO instrutores
VALUES (5, "Patricia", "pat@labenu.com", 16);


# DELETAR VALORES DA TABELA
DELETE FROM instrutores
WHERE id = 5;

 # UTILIZANDO OPERADORES => SPOILER DA PROXIMA AULA
DELETE FROM instrutores 
WHERE nome = "Tayanne" AND id = 6;

# CONSULTANDO VALORES
SELECT * FROM instrutores;

SELECT id, nome, email FROM instrutores
WHERE idade >= 25; 

# DESCREVE A ESTRUTURA DA TABELA
DESCRIBE instrutores;
